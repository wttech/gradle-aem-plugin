<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    {% if definition.description.orNull is not empty %}
    <comment>{{definition.description.orNull}}</comment>
    <entry key="description">{{definition.description.orNull}}</entry>
    {% endif %}
    <entry key="group">{{definition.group.get}}</entry>
    <entry key="name">{{definition.name.get}}</entry>
    <entry key="version">{{definition.version.get}}</entry>
    {% if definition.createdBy.orNull is not empty %}
    <entry key="createdBy">{{definition.createdBy.orNull}}</entry>
    {% endif %}
    {% for e in definition.properties.get %}
    <entry key="{{e.key}}">{{e.value | raw}}</entry>
    {% endfor %}
</properties>
